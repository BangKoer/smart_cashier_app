import 'package:flutter/material.dart';

class ProdutcsScreen extends StatelessWidget {
  const ProdutcsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ini Product Screens"),),
    );
  }
}